Functional Javascript workshop by Timoxley-
Completed this workshop by Timoxley which incudes the problems on async loops, 
basic call, basic every some, basic filter,basic inheritance with objectcreate, 
basic inheritance without objectcreate, basic map, basic recursion, basic reduce,
blocking event loop, currying, function call, function spies, higher order function,
implement map with reduce, partial application with bind, partial application without bind,
prototypical inheritance by hand and recursion.
